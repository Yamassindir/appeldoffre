import { UniquepipePipe } from './uniquepipe.pipe';

describe('UniquepipePipe', () => {
  it('create an instance', () => {
    const pipe = new UniquepipePipe();
    expect(pipe).toBeTruthy();
  });
});
